# Modulizer

Modulizer is a CLI tool used to generate thin C++20 module interface files around traditional C/C++ headers. The intent is to allow the creation of C++20 modules for third-party libraries without having to rewrite the headers.

## Usage

